{% use "bootstrap_4_layout.html.twig" %}

{% block phone_widget -%}
    <div class="input-group">
        <span class="input-group-text">{{ form.vars.phone_prefix }}</span>
        {{ form_widget(form, form.vars.attr) }}
    </div>
{% endblock %}