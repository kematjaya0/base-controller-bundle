{% block phone_widget -%}
    <div class="input-group">
        <span class="input-group-addon">{{ form.vars.phone_prefix }}</span>
        {{ form_widget(form, form.vars.attr) }}
    </div>
{% endblock %}